# 两本密码学教材系统化整理：讲义型展开提纲

  

## 一、为什么上一版看起来像名词罗列

  

上一版的问题不是“内容不够多”，而是“没有把知识之间的因果关系讲出来”。  

例如，只写：

  

> 对称密码：流密码、分组密码、AES、SM4、GCM、ChaCha20-Poly1305

  

这当然像关键词表。真正适合学习的提纲应该写成：

  

> 对称密码要解决的是“大量数据如何高效保密”的问题。  

> 它的优势是速度快，适合加密文件、数据库、网络流量；但它要求通信双方先共享密钥，因此引出密钥分发问题。  

> 在算法层面，对称密码分成流密码和分组密码。流密码像“一次一密”的工程近似，核心风险是密钥流复用；分组密码每次处理固定长度数据块，所以必须借助工作模式处理任意长度消息。  

> 仅仅加密不能防篡改，因此现代系统倾向使用认证加密 AEAD，如 AES-GCM、ChaCha20-Poly1305。  

> 因此，本章不是孤立讲 AES，而是讲“共享密钥如何安全地保护大量数据”。

  

这才是“提纲”，因为它告诉你每部分为什么存在、先讲什么、后讲什么、概念之间怎么连接。

  

---

  

## 二、整份笔记应该有一条总主线

  

这两本书整理出来的笔记，不应该按“算法名”堆叠，而应该围绕一个总问题：

  

> 在不可信环境中，如何让数据、身份、通信和计算保持可信？

  

这个总问题可以拆成五个连续问题：

  

```text

第一问：我要保护什么？

第二问：我用什么密码工具保护？

第三问：这些工具怎么组合成协议？

第四问：协议如何落到真实系统？

第五问：现代密码学还在解决哪些新问题？

```

  

对应到笔记结构就是：

  

| 层次 | 解决的问题 | 主要内容 |

|---|---|---|

| 安全目标层 | 为什么需要密码学 | 机密性、完整性、认证性、不可否认性、新鲜性、前向安全性 |

| 密码原语层 | 密码学提供哪些基础工具 | 对称加密、公钥加密、哈希、MAC、数字签名、密钥交换、KDF |

| 组合构造层 | 工具如何组合才安全 | AEAD、KEM-DEM、PKI、证书链、协议握手 |

| 协议系统层 | 真实系统如何使用密码 | TLS、HTTPS、Signal、Kerberos、FIDO2、区块链 |

| 前沿扩展层 | 新场景提出了什么新问题 | 后量子、零知识证明、MPC、FHE、门限密码、国密体系 |

  

这样组织后，AES、RSA、SM2、SM3、TLS、ZKP 就不再是一堆散点，而是各自有位置：

  

- AES / SM4：解决大量数据加密。

- SHA-256 / SM3：解决摘要和完整性基础。

- HMAC / KMAC：解决共享密钥下的消息认证。

- RSA / ECC / SM2：解决公钥加密、签名、密钥交换。

- TLS：把签名、证书、密钥交换、AEAD 组合成安全传输协议。

- ZKP / MPC / FHE：解决“在不泄露秘密的前提下证明或计算”的新问题。

  

---

  

## 三、两本书应该怎么合并

  

### 1. 《密码学引论》的作用

  

《密码学引论》更像传统本科教材，适合提供“密码学学科骨架”。  

它应该承担这些部分：

  

1. 密码学基础概念。

2. 古典密码。

3. 对称密码基本理论。

4. 分组密码和序列密码。

5. 密码分析思想。

6. 公钥密码数学基础。

7. 数字签名。

8. 认证与密钥管理。

9. 中国商用密码算法。

10. 可信计算等应用内容。

  

它的价值在于：  

**让你知道密码学作为一门学科，传统上是如何分类、如何定义问题、如何描述算法安全性的。**

  

### 2. 《深入浅出密码学》的作用

  

《深入浅出密码学》更像现代工程密码学读物，适合补充“真实世界怎么用”。  

它应该承担这些部分：

  

1. 现代哈希与 MAC。

2. 认证加密 AEAD。

3. Diffie-Hellman、ECDH、混合加密。

4. TLS。

5. Signal 等端到端加密协议。

6. 用户认证、PAKE、FIDO2。

7. 硬件密码、TEE、TPM、HSM。

8. 后量子密码。

9. 零知识证明、安全多方计算、同态加密等前沿。

  

它的价值在于：  

**让你知道密码算法不是孤立使用的，而是嵌入协议、软件、硬件和用户系统中。**

  

### 3. 合并原则

  

整理笔记时应采用：

  

```text

《密码学引论》负责“概念、分类、传统算法和数学基础”；

《深入浅出密码学》负责“工程实践、协议组合和现代前沿”；

互联网检索负责“标准变化、国密规范、后量子标准和现实部署”。

```

  

---

  

# 四、真正应该写出的总目录

  

下面这个目录不是名词表，而是“每一编都围绕一个问题展开”。

  

---

  

## 第一编：密码学到底保护什么

  

### 第 1 章：从信息安全目标理解密码学

  

这一章的重点不是列术语，而是建立一个最基本的判断框架：  

**看到一个密码技术，先问它保护的是哪一种安全目标。**

  

#### 1.1 为什么“加密”不等于“安全”

  

举例来说，如果攻击者看不懂一条银行转账消息：

  

```text

转账 100 元给张三

```

  

但他可以把密文篡改成另一段密文，使解密后变成：

  

```text

转账 900 元给张三

```

  

那么系统虽然有机密性，却没有完整性。  

所以安全系统通常不能只加密，还要认证和校验。

  

这一部分要讲清：

  

- 机密性：别人不能看懂。

- 完整性：别人不能偷偷改。

- 认证性：我知道消息来自谁。

- 不可否认性：发送者事后不能否认。

- 新鲜性：这不是旧消息重放。

- 前向安全性：长期密钥泄露后，过去通信仍尽量安全。

  

#### 1.2 重点展开：机密性、完整性、认证性为什么必须分开

  

这三个目标最容易被混为一谈。

  

| 安全目标 | 它防什么 | 它不一定防什么 | 典型技术 |

|---|---|---|---|

| 机密性 | 偷看 | 篡改、伪造 | 对称加密、公钥加密 |

| 完整性 | 内容被改 | 来源伪造 | 哈希、MAC、签名 |

| 认证性 | 冒充来源 | 内容保密 | MAC、数字签名、证书 |

| 不可否认性 | 事后抵赖 | 内容泄露 | 数字签名、时间戳 |

  

必须强调：  

**加密主要解决“看不看得懂”，认证解决“是不是你发的”，完整性解决“有没有被改”。**

  

#### 1.3 重点展开：密码学不能解决什么

  

密码学不是万能的。比如：

  

- HTTPS 不能防止你把密码主动输进钓鱼网站。

- 端到端加密不能防止对方截图。

- 数据库加密不能防止应用层把明文泄露出去。

- 数字签名不能证明签名者当时“主观愿意”，只能证明私钥参与了签名。

- 算法安全不能保证代码实现安全。

  

这一章结尾应该让读者建立一个意识：

  

> 密码学解决的是特定威胁模型下的特定安全目标，不是整个系统安全的代名词。

  

---

  

### 第 2 章：密码学的基本语言

  

这一章要解决的是：  

**同样是“保护数据”，编码、加密、哈希、MAC、签名到底有什么不同？**

  

#### 2.1 编码、加密、哈希、MAC、签名的核心差别

  

这部分必须详细展开，因为这是初学者最容易混淆的地方。

  

| 技术 | 是否需要密钥 | 是否可逆 | 解决的问题 | 例子 |

|---|---|---|---|---|

| 编码 | 不需要 | 可逆 | 数据格式转换 | Base64、URL Encoding |

| 加密 | 需要 | 可逆 | 机密性 | AES、SM4、RSA-OAEP |

| 哈希 | 不需要 | 不可逆 | 摘要、指纹 | SHA-256、SM3 |

| MAC | 需要共享密钥 | 不可逆 | 完整性 + 消息认证 | HMAC、KMAC、CMAC |

| 数字签名 | 私钥签名、公钥验证 | 不可逆 | 完整性 + 认证 + 不可否认 | RSA-PSS、ECDSA、SM2 |

  

这里要用例子解释：

  

- Base64 不是加密，因为任何人都能解码。

- MD5/SHA-256 不是加密，因为它们不能解密。

- 普通哈希不能证明消息是谁发的，因为攻击者也能重新算哈希。

- MAC 可以证明消息来自共享密钥持有者，但不能向第三方证明到底是哪一方发的。

- 数字签名可以由任何人验证，因为验证用的是公钥。

  

#### 2.2 重点展开：算法、原语、协议、系统

  

这一部分要让读者不要把 AES、TLS、HTTPS 混为一谈。

  

| 层级 | 含义 | 例子 |

|---|---|---|

| 密码原语 | 基础功能模块 | 加密、哈希、MAC、签名、密钥交换 |

| 密码算法 | 某个具体实现 | AES、SM4、RSA、SM2、SHA-256、SM3 |

| 密码构造 | 按规则组合算法 | AES-GCM、RSA-OAEP、HKDF |

| 密码协议 | 多方交互流程 | TLS、Signal、Kerberos |

| 密码系统 | 包含协议、实现、密钥、用户、硬件的整体 | HTTPS 生态、PKI、端到端加密应用 |

  

要重点强调：

  

> AES 是算法，不是协议。  

> TLS 是协议，不是算法。  

> HTTPS 是 HTTP 使用 TLS 的结果，不是一种加密算法。

  

#### 2.3 重点展开：Kerckhoffs 原则

  

这部分要讲出它为什么重要，而不是只写定义。

  

Kerckhoffs 原则说：  

**密码系统即使除了密钥以外全部公开，也应该保持安全。**

  

这背后的逻辑是：

  

1. 算法很难长期保密。

2. 公开算法可以接受学术界和工业界分析。

3. 密钥泄露可以更换，算法泄露很难整体替换。

4. 标准化算法便于互操作和审计。

  

所以“自创加密算法”“靠代码混淆保护算法”“闭源所以安全”都是危险想法。

  

---

  

## 第二编：古典密码为什么还要学

  

古典密码不应该作为重点算法学习，而应该作为“密码分析意识”的入口。

  

### 第 3 章：从古典密码理解攻击者思维

  

古典密码的价值不在于今天还能用，而在于它揭示了密码学的一个核心事实：

  

> 如果加密没有消除明文中的统计规律，攻击者就可能利用规律恢复信息。

  

#### 3.1 代替密码讲什么

  

代替密码的核心是“把一个符号换成另一个符号”。  

Caesar 密码、单表代换、Vigenere 密码都属于这一类。

  

要重点讲：

  

- Caesar 密码密钥空间太小，可以穷举。

- 单表代换虽然密钥空间大，但保留了语言频率特征。

- Vigenere 密码通过多表代换削弱频率分析，但仍可能通过周期分析破解。

  

这里应引出一个重要思想：

  

> 密钥空间大不一定安全；如果结构泄露严重，攻击者不需要穷举全部密钥。

  

#### 3.2 置换密码讲什么

  

置换密码不改变字符本身，而改变字符位置。  

它说明另一个基本思想：隐藏信息可以通过“打乱位置”实现。

  

但它的问题是：  

如果字符内容的统计特征还在，仅仅打乱位置往往不够。

  

这可以自然引到现代分组密码中的两个思想：

  

- 混淆：让密钥、明文、密文之间关系复杂。

- 扩散：让明文的一点变化影响密文很多位置。

  

#### 3.3 古典密码到现代密码的过渡

  

这一章最后要完成一个过渡：

  

```text

古典密码的问题：

依赖人工规则，结构简单，容易泄露统计特征。

  

现代密码的目标：

用严格的算法结构、密钥空间、安全模型和公开分析来抵抗攻击。

```

  

所以古典密码章节不是为了记住多少古典算法，而是为了理解：

  

- 为什么要有密码分析。

- 为什么“看起来乱”不等于安全。

- 为什么现代密码强调攻击模型和安全定义。

  

---

  

## 第三编：对称密码——大量数据如何高效保密

  

对称密码这一编要围绕一句话展开：

  

> 对称密码是现代系统加密大量数据的主力，但它必须解决密钥共享、工作模式、认证和 nonce 使用问题。

  

---

  

### 第 4 章：对称密码的基本逻辑

  

#### 4.1 为什么需要对称密码

  

现实系统中需要加密大量数据：

  

- 文件。

- 数据库。

- 磁盘。

- 网络流量。

- 视频流。

- 云端备份。

  

如果全部用 RSA 或 ECC 这种公钥密码处理，会非常低效。  

所以真实系统一般是：

  

```text

公钥密码负责建立会话密钥；

对称密码负责加密大量数据。

```

  

这就是“混合加密”的基本逻辑。

  

#### 4.2 对称密码的核心矛盾

  

对称密码的优点是快，缺点是通信双方必须先共享同一把密钥。

  

这个矛盾引出后续两条线：

  

1. 如何用对称算法安全加密数据？

   - 流密码。

   - 分组密码。

   - 工作模式。

   - AEAD。

  

2. 如何让双方安全获得同一把密钥？

   - Diffie-Hellman。

   - 公钥加密。

   - KEM。

   - TLS 握手。

  

#### 4.3 重点区分：流密码 vs 分组密码

  

这一部分必须重点展开。

  

| 对比点 | 流密码 | 分组密码 |

|---|---|---|

| 基本方式 | 生成密钥流，与明文结合 | 每次处理固定长度数据块 |

| 直观类比 | 工程版“一次一密” | 伪随机置换机器 |

| 典型算法 | ChaCha20、ZUC、历史上的 RC4 | AES、SM4、DES |

| 核心风险 | 密钥流复用 | 模式误用、填充错误、ECB 泄露结构 |

| 常见组合 | ChaCha20-Poly1305 | AES-GCM、AES-CBC、AES-CTR |

  

流密码的核心直觉是：

  

```text

密文 = 明文 XOR 密钥流

```

  

如果同一个密钥流被使用两次：

  

```text

C1 = P1 XOR K

C2 = P2 XOR K

C1 XOR C2 = P1 XOR P2

```

  

攻击者虽然不知道 K，但能得到两个明文之间的关系。  

这就是为什么流密码和 CTR/GCM 一类模式都极其强调 nonce 不重复。

  

#### 4.4 重点区分：算法 vs 工作模式

  

AES 本身只能处理固定长度分组。  

但现实消息可能是任意长度，所以需要工作模式。

  

| 模式 | 直观解释 | 主要问题或特点 |

|---|---|---|

| ECB | 每个块独立加密 | 相同明文块得到相同密文块，泄露结构，通常不应使用 |

| CBC | 前一块密文影响后一块 | 需要随机 IV，还需要 MAC 防篡改 |

| CTR | 把分组密码变成密钥流 | nonce/counter 不能重复 |

| GCM | CTR 加密 + 认证 | 高效 AEAD，但 nonce 重复很危险 |

  

必须强调：

  

> 选择 AES 不等于系统安全。  

> AES 的模式、nonce、padding、认证方式同样决定安全性。

  

---

  

### 第 5 章：AES、SM4 与分组密码思想

  

这一章不是机械介绍 AES 的每一步，而是要讲分组密码为什么能安全。

  

#### 5.1 分组密码的抽象模型

  

可以把分组密码理解成：

  

> 在密钥控制下，把固定长度输入块映射成固定长度输出块的可逆伪随机置换。

  

也就是说，对不知道密钥的人来说，AES 的输出应该像随机置换一样不可预测。

  

#### 5.2 AES 要讲什么

  

AES 部分不需要把所有有限域运算证明都展开，但要讲清：

  

- AES 是分组密码。

- 分组长度是 128 位。

- 密钥长度可以是 128、192、256 位。

- 内部多轮变换体现混淆和扩散。

- AES 不是直接“加密任意文件”的完整方案，需要配合模式。

- AES-GCM 是现代协议中常见的认证加密方案。

  

#### 5.3 SM4 要讲什么

  

SM4 要放在中国商用密码体系中理解。

  

要讲清：

  

- SM4 是分组密码。

- 它主要对应 AES 在国密体系中的角色。

- 它用于数据加密。

- 它和 SM2、SM3 的功能完全不同：

  - SM2 是公钥密码。

  - SM3 是哈希。

  - SM4 是对称分组密码。

  

#### 5.4 重点区分：DES、AES、SM4

  

| 算法 | 类型 | 地位 | 现在如何看待 |

|---|---|---|---|

| DES | 分组密码 | 历史经典 | 密钥太短，不适合现代安全 |

| 3DES | 分组密码 | 过渡方案 | 性能和安全边际不足，逐渐淘汰 |

| AES | 分组密码 | 国际主流 | 现代通用标准 |

| SM4 | 分组密码 | 中国商用密码标准 | 国密体系中重要对称算法 |

  

---

  

## 第四编：哈希、MAC 与认证加密——如何发现篡改、确认来源

  

这一编要围绕一个核心问题：

  

> 加密解决“别人看不看得懂”，但系统还必须解决“别人有没有改过、是不是你发的”。

  

---

  

### 第 6 章：哈希函数不是加密

  

#### 6.1 哈希函数的核心作用

  

哈希函数把任意长度输入变成固定长度摘要。  

它像数据的“指纹”。

  

它常用于：

  

- 文件完整性校验。

- 数字签名前摘要。

- Merkle 树。

- 区块链。

- 承诺方案。

- 数据索引和去重。

- 口令存储中的基础组件。

  

但哈希不是加密，因为它不可逆。  

不能说“SHA-256 解密”。

  

#### 6.2 三个安全性质必须分清

  

| 性质 | 问题形式 | 直观解释 |

|---|---|---|

| 抗第一原像 | 给定 h，找 m 使 H(m)=h 很难 | 看到指纹，很难反推出原物 |

| 抗第二原像 | 给定 m1，找 m2 使 H(m1)=H(m2) 很难 | 给定一个文件，很难造另一个同摘要文件 |

| 抗碰撞 | 找任意 m1、m2 使 H(m1)=H(m2) 很难 | 很难随便找出两个同摘要输入 |

  

这里必须讲清：

  

> 抗碰撞不等于抗原像。  

> 碰撞攻击不是“从哈希值还原原文”，而是找两个不同输入产生同一摘要。

  

#### 6.3 普通哈希为什么不能认证来源

  

如果你发布一个文件和它的 SHA-256：

  

```text

file.zip

SHA-256 = abc...

```

  

别人下载后可以校验文件是否损坏。  

但如果攻击者能同时替换文件和哈希值，那普通哈希就无能为力。

  

所以：

  

- 普通哈希：检测变化。

- MAC：在共享密钥下检测篡改并认证来源。

- 数字签名：在公钥体系下检测篡改并认证来源。

  

---

  

### 第 7 章：MAC 与 AEAD

  

#### 7.1 MAC 解决什么问题

  

MAC 的核心问题是：

  

> 我和你共享一把密钥。你收到消息后，怎么确认这条消息确实来自共享密钥持有者，并且中途没被改？

  

MAC 输出一个 tag：

  

```text

tag = MAC(key, message)

```

  

接收者重新计算 tag，如果一致，就认为消息通过认证。

  

#### 7.2 MAC 和哈希的区别

  

| 对比 | 哈希 | MAC |

|---|---|---|

| 是否有密钥 | 没有 | 有 |

| 谁能计算 | 任何人 | 只有密钥持有者 |

| 能否认证来源 | 不能 | 可以在共享密钥范围内认证 |

| 典型算法 | SHA-256、SM3 | HMAC、KMAC、CMAC |

  

这里要重点讲：

  

> Hash(message) 不能防伪造，因为攻击者也能算。  

> MAC(key, message) 可以防伪造，因为攻击者没有 key。

  

#### 7.3 MAC 和数字签名的区别

  

MAC 的问题是双方共享同一密钥。  

因此第三方看到一个合法 MAC 时，无法判断到底是 Alice 生成的，还是 Bob 生成的，因为两人都有同一把密钥。

  

数字签名则不同：

  

- 私钥只有签名者有。

- 公钥任何人都能验证。

- 因此可以支持不可否认性。

  

所以：

  

| 对比 | MAC | 数字签名 |

|---|---|---|

| 密钥模型 | 共享密钥 | 私钥签名，公钥验证 |

| 验证者 | 共享密钥持有者 | 任何知道公钥的人 |

| 性能 | 快 | 相对慢 |

| 不可否认性 | 通常没有 | 通常有 |

| 用途 | 通信双方消息认证 | 软件签名、证书、交易签名 |

  

#### 7.4 AEAD 为什么重要

  

过去常见错误是“只加密不认证”。  

攻击者虽然看不懂密文，但仍可能修改密文，诱导接收方解出错误明文。

  

AEAD，也就是认证加密，同时提供：

  

- 机密性。

- 完整性。

- 消息认证。

- 可选的附加认证数据 AAD。

  

典型方案：

  

- AES-GCM。

- ChaCha20-Poly1305。

  

这一章要形成一个重要结论：

  

> 现代应用中，除非有非常明确理由，否则应优先使用标准 AEAD，而不是手动拼接“加密 + 哈希”。

  

---

  

## 第五编：公钥密码——陌生双方如何建立安全关系

  

这一编要围绕一个问题展开：

  

> 如果两个人从未共享过秘密密钥，如何在不安全网络上建立安全通信？

  

---

  

### 第 8 章：公钥密码的核心思想

  

#### 8.1 公钥密码解决的不是“大量数据加密”

  

公钥密码常被初学者误解为“比对称密码更高级的加密”。  

实际上它更重要的作用是：

  

1. 密钥交换。

2. 数字签名。

3. 公钥加密小数据或会话密钥。

4. 建立身份与信任。

  

真实系统通常不会用 RSA/ECC 直接加密大文件，而是：

  

```text

用公钥密码建立或封装一个会话密钥；

用对称密码加密实际数据。

```

  

#### 8.2 公钥密码的数学直觉

  

公钥密码依赖“正向容易，反向困难”的问题。

  

| 机制 | 正向容易 | 反向困难 |

|---|---|---|

| RSA | 两个大素数相乘 | 从乘积中分解出素数 |

| DH | 做模幂运算 | 求离散对数 |

| ECC | 做椭圆曲线标量乘法 | 求椭圆曲线离散对数 |

| 格密码 | 构造带噪声线性关系 | 从噪声中恢复秘密 |

  

这部分要讲清“陷门”的直觉：

  

> 有些函数正向计算容易，反向求解困难；但如果掌握特殊秘密信息，就能高效反向求解。

  

RSA 是典型陷门函数思想。

  

#### 8.3 公钥加密、签名、密钥交换必须分开

  

这是重点中的重点。

  

| 功能 | 目的 | 典型算法 |

|---|---|---|

| 公钥加密 | 让只有私钥持有者能解密 | RSA-OAEP、ECIES、SM2 加密 |

| 密钥交换 | 双方协商共享秘密 | DH、ECDH、SM2 密钥交换 |

| 数字签名 | 证明消息由私钥持有者签发 | RSA-PSS、ECDSA、EdDSA、SM2 签名 |

  

不能把它们混成一句“非对称加密”。

  

---

  

### 第 9 章：RSA、DH、ECC、SM2 应该怎么讲

  

#### 9.1 RSA 不要讲成“私钥加密、公钥解密”

  

很多教材或入门材料会说“RSA 签名就是私钥加密，公钥解密”。  

这是一种容易误导的说法。

  

更准确地说：

  

- RSA 加密和 RSA 签名是两种不同方案。

- 它们使用不同填充。

- 它们满足不同安全定义。

- 工程中不能直接使用“裸 RSA”。

  

应分别讲：

  

| RSA 用途 | 应该使用的方案 | 目标 |

|---|---|---|

| 加密 | RSA-OAEP | 机密性 |

| 签名 | RSA-PSS | 完整性、认证、不可否认 |

  

#### 9.2 Diffie-Hellman 的重点是“协商”，不是“加密”

  

DH 的故事应该这样讲：

  

1. Alice 选一个秘密数 a。

2. Bob 选一个秘密数 b。

3. 双方交换公开值。

4. Alice 和 Bob 都能算出同一个共享秘密。

5. 旁观者只能看到公开值，很难算出共享秘密。

  

但必须强调：

  

> 纯 DH 不自动认证对方身份，可能被中间人攻击。

  

所以真实协议会把 DH 和签名、证书或预共享密钥结合起来，形成认证密钥交换。

  

#### 9.3 ECC 的重点是“同样安全强度下密钥更短”

  

ECC 不是“更短的 RSA”，而是基于完全不同的数学结构：椭圆曲线群。

  

应该讲清：

  

- ECC 基于椭圆曲线离散对数困难问题。

- ECDH 用于密钥交换。

- ECDSA / EdDSA 用于签名。

- SM2 是中国商用密码体系中的椭圆曲线公钥算法。

- ECC 常在相同安全强度下比 RSA 使用更短密钥，适合移动端、证书、协议握手等场景。

  

---

  

## 第六编：数字签名、证书与 PKI——如何知道公钥属于谁

  

这一编要围绕一个问题：

  

> 数字签名能证明“某个私钥签过”，但你怎么知道这个私钥属于谁？

  

---

  

### 第 10 章：数字签名

  

#### 10.1 数字签名解决什么

  

数字签名解决三个问题：

  

1. 消息有没有被改。

2. 消息是不是由某个私钥持有者签发。

3. 签名者事后能否否认。

  

它通常不解决机密性。  

签名后的消息默认仍然是可见的，除非另行加密。

  

#### 10.2 为什么通常是“先哈希再签名”

  

实际签名通常不是直接签整个大文件，而是：

  

```text

digest = Hash(message)

signature = Sign(private_key, digest)

```

  

原因是：

  

- 消息可能很大。

- 签名算法输入长度有限。

- 哈希能把任意长度消息压缩成固定长度摘要。

- 签名绑定的是摘要，摘要再绑定原文。

  

但这也意味着哈希函数必须安全。  

如果哈希函数可碰撞，攻击者可能构造两个不同文件共享同一摘要，进而滥用签名。

  

#### 10.3 数字签名与 MAC 的关键区别

  

这部分必须反复强调：

  

- MAC 适合双方共享密钥的场景。

- 数字签名适合公开验证的场景。

- MAC 没有天然不可否认性。

- 数字签名通常可由第三方验证。

  

典型场景：

  

| 场景 | 更适合 |

|---|---|

| 两台服务器之间高速认证消息 | MAC |

| 软件发布者给安装包签名 | 数字签名 |

| 区块链交易授权 | 数字签名 |

| TLS 证书链验证 | 数字签名 |

| 内部 API 请求认证 | HMAC 或类似 MAC |

  

---

  

### 第 11 章：证书与 PKI

  

#### 11.1 为什么有了签名还需要证书

  

假设你访问一个网站，服务器发给你一个公钥。  

问题是：  

**你怎么知道这个公钥真的属于这个网站，而不是中间人伪造的？**

  

证书就是为了解决这个问题：

  

> 证书是由可信 CA 签名的数据结构，用来绑定“身份”和“公钥”。

  

证书里通常包含：

  

- 域名或主体身份。

- 公钥。

- 有效期。

- 签发者。

- 签名算法。

- CA 的数字签名。

  

#### 11.2 PKI 的核心逻辑

  

PKI 的逻辑链是：

  

```text

操作系统/浏览器信任根 CA；

根 CA 签发中间 CA；

中间 CA 签发网站证书；

浏览器验证证书链；

证书中的公钥用于认证 TLS 握手。

```

  

这部分要强调：

  

> 证书不是“加密网页”的东西，而是证明公钥归属的东西。

  

#### 11.3 证书验证要讲哪些点

  

不能只写“验证证书”，要展开：

  

1. 证书链是否能连到受信任根。

2. 证书是否在有效期内。

3. 域名是否匹配。

4. 签名是否正确。

5. 是否被撤销。

6. 用途扩展是否允许当前用途。

7. 算法和密钥长度是否符合安全要求。

  

---

  

## 第七编：密码协议——安全不是把算法拼起来

  

这一编要围绕一句话：

  

> 安全算法错误组合后，仍然可能形成不安全协议。

  

---

  

### 第 12 章：密码协议的思维方式

  

#### 12.1 协议要考虑交互过程

  

协议不是单个公式，而是多方交换消息的过程。  

攻击者可能：

  

- 窃听。

- 篡改。

- 删除。

- 重放。

- 冒充。

- 降级算法。

- 插入中间人。

- 利用错误信息。

  

所以协议设计要考虑消息顺序、身份绑定、密钥确认、上下文绑定和错误处理。

  

#### 12.2 密钥交换和认证密钥交换的区别

  

普通密钥交换只保证双方得到共享秘密。  

但如果没有认证，中间人可以分别和双方建立不同密钥。

  

认证密钥交换还要保证：

  

- 我知道共享密钥是和谁建立的。

- 对方身份没有被替换。

- 密钥和上下文绑定。

- 攻击者不能降级算法或重放旧消息。

  

---

  

### 第 13 章：TLS / HTTPS 应该怎么讲

  

TLS 是最适合做“综合案例”的协议，因为它几乎组合了前面所有核心概念：

  

| TLS 组件 | 对应密码知识 |

|---|---|

| 证书 | PKI、公钥归属 |

| 签名 | 服务器身份认证 |

| ECDHE | 密钥交换、前向安全 |

| HKDF | 密钥派生 |

| AEAD | 应用数据加密与认证 |

| 记录层 | 传输数据保护 |

| 握手层 | 参数协商、身份认证、密钥建立 |

  

TLS 的学习主线应该是：

  

```text

浏览器要和服务器安全通信。

首先要确认服务器是谁；

然后协商一个会话密钥；

再用这个会话密钥保护后续数据；

同时要防止中间人、重放和降级攻击。

```

  

这一章必须区分：

  

| 概念 | 解释 |

|---|---|

| TLS vs HTTPS | TLS 是安全传输协议；HTTPS 是 HTTP over TLS |

| TLS vs 端到端加密 | TLS 到服务器终止；端到端加密连服务器也看不到明文 |

| 证书认证 vs 数据加密 | 证书证明公钥归属；AEAD 保护数据 |

| 握手密钥 vs 应用数据密钥 | 握手建立密钥材料，记录层使用派生出的数据密钥 |

  

---

  

### 第 14 章：Signal 与端到端加密

  

Signal 这一章不能只列 X3DH、Double Ratchet，而要讲它解决的场景：

  

> 两个用户可能不同时在线，但仍要建立端到端加密通信；服务器负责转发消息，但不应该看到明文。

  

因此它需要：

  

1. 长期身份密钥：证明用户身份。

2. 预密钥：支持异步建立会话。

3. X3DH：初始密钥协商。

4. Double Ratchet：持续更新密钥。

5. 前向安全：过去消息尽量不受未来密钥泄露影响。

6. 后泄露安全：某次密钥泄露后，后续通信可以逐步恢复安全。

  

这里要重点区分：

  

| 概念 | 解释 |

|---|---|

| TLS | 用户到服务器安全 |

| E2EE | 用户到用户安全 |

| 前向安全 | 保护过去 |

| 后泄露安全 | 泄露后重新恢复安全 |

| 服务器可信 | TLS 中服务器可见明文；E2EE 中服务器只转发密文 |

  

---

  

## 第八编：密钥管理与密码工程——真正容易出事的地方

  

这一编要围绕一句话：

  

> 密码系统最常见的失败原因不是算法被数学攻破，而是密钥、随机数、实现和配置出了问题。

  

---

  

### 第 15 章：密钥管理

  

#### 15.1 密钥生命周期

  

密钥不是生成出来就完事。  

完整生命周期包括：

  

1. 生成。

2. 分发。

3. 存储。

4. 使用。

5. 轮换。

6. 备份。

7. 撤销。

8. 销毁。

9. 审计。

  

每一步都可能出问题。

  

#### 15.2 密钥生成和密钥派生的区别

  

| 概念 | 说明 |

|---|---|

| 密钥生成 | 从密码学安全随机数产生新密钥 |

| 密钥派生 | 从已有秘密材料派生多个用途不同的密钥 |

| KDF | 密钥派生函数 |

| HKDF | 常见的基于 HMAC 的密钥派生函数 |

| salt | 增加唯一性，避免预计算攻击 |

| context/info | 把密钥绑定到具体用途，避免混用 |

  

要重点讲：

  

> 同一个密钥不应在多个用途之间随便复用。  

> 加密密钥、MAC 密钥、握手密钥、应用数据密钥应分离。

  

#### 15.3 密钥存在哪里

  

密钥可以放在：

  

- 配置文件。

- 环境变量。

- KMS。

- HSM。

- TPM。

- TEE。

- 智能卡。

- 操作系统密钥库。

  

这一章要强调：

  

> 密钥管理是密码系统的中心，不是附属问题。

  

---

  

### 第 16 章：密码工程与常见误用

  

这一章应该写得非常具体，因为它最能提高实际能力。

  

#### 16.1 常见错误

  

| 错误 | 后果 |

|---|---|

| 使用 ECB 模式 | 相同明文块泄露相同结构 |

| GCM nonce 重复 | 可能严重破坏机密性和完整性 |

| 不验证证书 | TLS 可能被中间人攻击 |

| 使用普通随机数生成密钥 | 密钥可预测 |

| 密钥硬编码 | 代码泄露即密钥泄露 |

| 自己拼接加密和哈希 | 容易组合错误 |

| 错误处理泄露细节 | 可能导致填充 oracle 等攻击 |

| 非恒定时间比较 MAC | 时间侧信道 |

  

#### 16.2 数学安全和实现安全的区别

  

AES、RSA、ECC 本身经过充分分析，不代表某个软件实现安全。  

攻击者可能不从数学上破解，而是观察：

  

- 运算时间。

- 缓存访问。

- 功耗变化。

- 电磁泄露。

- 错误输出。

- 异常信息。

- 随机数质量。

  

这部分要讲清：

  

> 密码学从来不只是数学，还包括工程、硬件、实现和运维。

  

---

  

## 第九编：应用场景——把密码学放回真实系统

  

这一编要避免写成“应用名词表”，而是按场景提出问题。

  

---

  

### 第 17 章：区块链中的密码学

  

区块链不应该讲成“区块链都是加密的”。  

事实上，大多数公链数据是公开的。

  

区块链主要使用的是：

  

- 哈希函数：连接区块、构造地址、工作量证明。

- Merkle 树：高效证明交易属于某个区块。

- 数字签名：证明交易由私钥控制者授权。

- 共识机制：决定全网接受哪条链。

- 零知识证明：在部分系统中增强隐私或扩容。

  

必须重点区分：

  

| 错误理解 | 正确理解 |

|---|---|

| 区块链数据都是加密的 | 很多链上数据是公开可见的 |

| 哈希保证数据不能改 | 哈希让篡改可被发现，共识让篡改代价极高 |

| 地址等于真实身份 | 地址是公钥/哈希相关标识，不天然等于实名身份 |

| 签名就是加密 | 签名证明授权，不隐藏交易内容 |

  

---

  

### 第 18 章：云、数据库、物联网和移动通信

  

这一章要按场景讲“威胁模型不同，所以密码方案不同”。

  

#### 云计算

  

核心问题：

  

- 云服务商是否能看到数据？

- 密钥由谁控制？

- 数据在传输、存储、使用中分别如何保护？

  

涉及：

  

- 传输加密。

- 存储加密。

- KMS。

- HSM。

- 机密计算。

- 客户自持密钥。

  

#### 数据库

  

核心问题：

  

- 数据库被拖库后能否保护字段？

- 应用查询功能和加密如何权衡？

  

涉及：

  

- 透明数据加密。

- 字段级加密。

- 确定性加密。

- 可搜索加密。

- 泄露轮廓。

  

#### 物联网

  

核心问题：

  

- 设备资源有限。

- 容易被物理接触。

- 固件难以及时更新。

- 生命周期长。

  

涉及：

  

- 轻量密码。

- 设备身份。

- 安全启动。

- 固件签名。

- 密钥注入。

- 远程更新。

  

#### 移动通信

  

核心问题：

  

- 用户和基站之间如何认证。

- 空口如何加密。

- 完整性如何保护。

  

涉及：

  

- AKA。

- ZUC。

- 空口加密。

- 完整性算法。

  

---

  

## 第十编：现代密码学前沿——新问题不是简单加密

  

这一编要围绕“新型隐私需求”展开，而不是罗列前沿名词。

  

---

  

### 第 19 章：后量子密码

  

后量子密码要解决的问题是：

  

> 如果大规模量子计算机出现，现有 RSA、DH、ECC 这类公钥密码可能不再安全，那互联网如何迁移？

  

这里要讲清三个层次：

  

1. 量子算法威胁什么。

   - Shor 算法威胁 RSA、DH、ECC。

   - Grover 算法降低对称密钥搜索强度，但可通过加大参数缓解。

  

2. 后量子密码是什么。

   - 它不是“在量子计算机上运行的密码”。

   - 它是在经典计算机上运行，但希望抵抗量子攻击的密码。

  

3. 迁移为什么困难。

   - 算法替换。

   - 证书格式。

   - 协议兼容。

   - 硬件性能。

   - 密文和签名尺寸。

   - 长期机密性。

   - 先收集、后解密攻击。

  

必须区分：

  

| 概念 | 解释 |

|---|---|

| 后量子密码 | 经典计算机运行，抵抗量子攻击 |

| 量子密码 | 利用量子物理机制，如 QKD |

| KEM | 封装共享秘密的机制 |

| 传统 DH | 双方交互式协商共享秘密 |

  

---

  

### 第 20 章：零知识证明、MPC、FHE

  

这一章要用“隐私计算三问”组织：

  

#### 20.1 零知识证明：如何证明而不泄露

  

问题：

  

> 我如何证明我知道一个秘密，或者某个命题为真，但不泄露秘密本身？

  

应用：

  

- 区块链隐私交易。

- 身份证明。

- 合规证明。

- 可验证计算。

  

重点性质：

  

- 完备性。

- 可靠性。

- 零知识性。

  

#### 20.2 安全多方计算：如何共同算而不暴露输入

  

问题：

  

> 多个机构如何共同计算一个结果，但各自不泄露原始数据？

  

应用：

  

- 联合风控。

- 医疗数据分析。

- 隐私集合求交。

- 联合统计。

  

#### 20.3 全同态加密：如何在密文上计算

  

问题：

  

> 能不能把数据加密后交给别人计算，而别人始终看不到明文？

  

应用：

  

- 云端密文计算。

- 隐私机器学习。

- 受保护数据分析。

  

必须区分：

  

| 技术 | 核心问题 |

|---|---|

| ZKP | 证明一个命题，不泄露证明细节 |

| MPC | 多方一起算，不暴露各自输入 |

| FHE | 在密文上算，解密后得到正确结果 |

| TEE | 用硬件隔离保护运行时数据 |

  

---

  

## 第十一编：中国商用密码体系 / 国密体系

  

这一编不能写成“SM1、SM2、SM3、SM4 背编号”，而要写成“国密体系中各算法承担什么角色”。

  

### 第 21 章：中国商用密码体系

  

#### 21.1 先讲体系，不先讲编号

  

中国商用密码体系包括：

  

- 算法。

- 标准。

- 产品。

- 协议。

- 测评。

- 法规。

- 应用生态。

  

因此“国密”不是单个算法，而是一套体系。

  

#### 21.2 国密算法功能图

  

| 算法 | 类型 | 主要功能 | 类比理解 |

|---|---|---|---|

| SM1 | 对称分组密码 | 数据加密，常用于专用设备 | 类似 AES 的用途，但细节通常不公开 |

| SM2 | 椭圆曲线公钥密码 | 签名、密钥交换、公钥加密 | 类似 ECDSA/ECDH/ECIES 的组合角色 |

| SM3 | 哈希函数 | 摘要、完整性、签名前哈希 | 类似 SHA-256 |

| SM4 | 对称分组密码 | 数据加密 | 类似 AES |

| SM7 | 对称分组密码 | 特定行业应用 | 行业专用分组密码 |

| SM9 | 标识密码 | 基于身份的加密、签名、密钥交换 | IBE/IBS 思路 |

| ZUC | 序列密码 | 移动通信中的加密和完整性保护 | 流密码 |

  

#### 21.3 必须重点区分

  

| 容易混淆 | 正确理解 |

|---|---|

| SM2 vs SM3 | SM2 是公钥密码，SM3 是哈希 |

| SM3 vs SM4 | SM3 做摘要，SM4 做加密 |

| SM2 vs RSA | SM2 基于椭圆曲线，不是 RSA |

| SM4 vs ZUC | SM4 是分组密码，ZUC 是序列密码 |

| 国密算法 vs 国密体系 | 算法只是体系的一部分 |

  

#### 21.4 国密与前面知识的对应关系

  

| 通用密码学概念 | 国际常见算法 | 国密对应 |

|---|---|---|

| 分组密码 | AES | SM4 |

| 哈希函数 | SHA-256、SHA-3 | SM3 |

| 椭圆曲线签名 | ECDSA、EdDSA | SM2 签名 |

| 密钥交换 | ECDH | SM2 密钥交换 |

| 公钥加密 | RSA-OAEP、ECIES | SM2 加密 |

| 流密码 | ChaCha20、SNOW 3G | ZUC |

  

这一章最后要形成一个理解：

  

> 学国密不能只背编号，要把每个 SM 算法放到通用密码学功能框架中理解。

  

---

  

# 五、整份笔记最后必须有“概念辨析总章”

  

这部分应该作为复习核心，而不是附录随便列一下。

  

## 1. 对称加密 vs 非对称加密

  

要从以下角度展开：

  

- 密钥数量。

- 速度。

- 典型用途。

- 密钥分发问题。

- 是否适合大数据。

- 在 TLS 中如何配合。

  

结论：

  

> 对称加密负责高效保护数据；非对称密码负责解决密钥建立、身份认证和公开验证问题。

  

## 2. 流密码 vs 分组密码

  

要从以下角度展开：

  

- 数据处理单位。

- 是否需要工作模式。

- nonce/IV 的作用。

- 密钥流复用风险。

- AES-CTR 为什么像流密码。

- ChaCha20 和 AES-GCM 的工程差异。

  

结论：

  

> 流密码和分组密码不是高级低级之分，而是处理数据方式不同。

  

## 3. 哈希 vs MAC vs 数字签名

  

要从以下角度展开：

  

| 技术 | 有无密钥 | 谁能生成 | 谁能验证 | 主要功能 |

|---|---|---|---|---|

| 哈希 | 无 | 任何人 | 任何人 | 摘要 |

| MAC | 共享密钥 | 密钥持有者 | 密钥持有者 | 消息认证 |

| 数字签名 | 私钥/公钥 | 私钥持有者 | 任何有公钥者 | 公开验证和不可否认 |

  

结论：

  

> 哈希是指纹，MAC 是共享密钥下的认证，签名是公钥体系下的认证。

  

## 4. RSA 加密 vs RSA 签名

  

要从以下角度展开：

  

- 目标不同。

- 密钥使用方式不同。

- 填充方案不同。

- 安全定义不同。

- 工程 API 不同。

- 不能用“私钥加密”简单解释签名。

  

结论：

  

> RSA 加密和 RSA 签名是两个不同密码方案，不是同一操作反过来用。

  

## 5. 密码算法 vs 密码协议

  

要从以下角度展开：

  

- AES、RSA、SM3 是算法。

- TLS、Signal、Kerberos 是协议。

- 协议会组合多个算法。

- 协议安全取决于消息顺序、身份绑定、密钥确认和错误处理。

  

结论：

  

> 安全协议不是把安全算法拼起来，而是把算法放进经过分析的交互流程。

  

---

  

# 六、后续每一章应该怎么写

  

每一章都应按这个模板展开：

  

```markdown

# 第 X 章：章节标题

  

## 1. 本章主线

这一章解决什么问题？为什么它在整个密码学体系中重要？

  

## 2. 教材依据

- 《密码学引论》对应内容

- 《深入浅出密码学》对应内容

- 额外补充内容

  

## 3. 先讲直觉

用一个具体场景说明为什么需要这一技术。

  

## 4. 再讲定义

给出概念定义，但不要一上来堆定义。

  

## 5. 展开核心机制

说明它如何工作，必要时给出流程。

  

## 6. 讲典型算法

列算法，但每个算法都要说明它解决什么问题、现在是否推荐、在什么场景用。

  

## 7. 讲容易混淆的概念

至少比较 2–4 组相关概念。

  

## 8. 讲工程注意事项

说明真实系统中最容易错在哪里。

  

## 9. 学习重点

列本章最值得掌握的 3–5 个点。

  

## 10. 常见误区

列初学者最容易犯的错。

  

## 11. 自测问题

给出复习题。

```

  

---

  

# 七、最终总提纲压缩版

  

如果压缩成一页纸，整套笔记应是：

  

```text

第一编：密码学保护什么

- 安全目标

- 基础术语

- 编码、加密、哈希、MAC、签名辨析

  

第二编：古典密码为什么还要学

- 代替、置换

- 频率分析

- 从古典密码过渡到混淆与扩散

  

第三编：对称密码

- 对称密码的作用

- 流密码 vs 分组密码

- AES、SM4、ZUC

- 工作模式

- AEAD

  

第四编：哈希、MAC、认证加密

- 哈希安全性质

- HMAC、KMAC、CMAC

- MAC vs 签名

- AEAD

  

第五编：公钥密码

- 公钥密码的作用

- RSA、DH、ECC、SM2

- KEM-DEM

- 混合加密

  

第六编：数字签名与 PKI

- 签名的功能

- RSA-PSS、ECDSA、EdDSA、SM2

- 证书

- CA

- Web PKI

  

第七编：密码协议

- 协议安全思维

- TLS/HTTPS

- Signal/E2EE

- Kerberos、PAKE、FIDO2

  

第八编：密钥管理与密码工程

- 密钥生命周期

- KDF/HKDF

- HSM/TPM/TEE

- 侧信道

- 常见误用

  

第九编：应用场景

- 区块链

- 云计算

- 数据库

- 物联网

- 移动通信

  

第十编：现代前沿

- 后量子密码

- 零知识证明

- 安全多方计算

- 全同态加密

- 门限密码

  

第十一编：中国商用密码体系

- SM1、SM2、SM3、SM4、SM7、SM9、ZUC

- 国密算法功能对应

- 国密协议与应用

  

第十二编：总复习

- 高频概念辨析

- 算法功能表

- 协议流程表

- 常见误区表

```

  

---

  

# 八、这版提纲的核心变化

  

新版不再把密码学整理成：

  

```text

AES、RSA、ECC、SM3、TLS、ZKP……

```

  

而是整理成：

  

```text

为什么需要安全目标；

每个密码原语解决哪个目标；

多个原语如何组合成协议；

协议如何进入真实系统；

现代前沿又在解决哪些传统密码学解决不了的新问题。

```

  

这才是一份适合学习、复习和后续扩展的密码学知识笔记总纲。