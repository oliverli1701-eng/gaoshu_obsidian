服务器启动
    │
    ├──[第1拨] MySQLCandidateProvider  连MySQL 查6条DISTINCT
    │          ↓
    │     CandidateInventory (Python内存，永久缓存)
    │          │
    │      用途：给规则引擎做"名字匹配"用
    │      (知道数据库里有哪些老师/学院/课程名)
    │
    │      ★ 这6条SQL从此不再执行 ★
    │


用户发消息 "榆中校区信息院周五的课"
    │
    ├── 规则引擎拿着 Python 内存里的 CandidateInventory
    │   做匹配："信息院" → "信息科学与工程学院"  (没碰MySQL)
    │   输出: {campus_name: "榆中校区", college_name: "信息科学与工程学院", time_place: "周五"}
    │
    ├──[第2拨] MySQLCourseRepository 拿着这条件
    │         连MySQL 发 SELECT ... WHERE campus_name=... AND college_name=...
    │         拿到真实的课程记录 (没缓存，每次查)
    │
    └── 拼进 LLM prompt

# 候选词表缓存机制分析

  

## 核心结论

  

对，就是我说的意思——**候选词表的查询结果一直在 Python 进程里**，随着 uvicorn 的运行存活，直到服务器重启才消失。

  

MySQL 还是那个 MySQL，每次请求查具体课程的时候照常连库，但候选词表查询结果不再重复获取。

  

## 代码证据

  

### 启动时加载一次

  

`server.py:24`：

  

```python

runtime = build_mysql_website_runtime_from_env(".env")

```

  

这行在模块顶层，FastAPI 进程一启动就执行。`CandidateInventory` 作为 `runtime.candidate_provider` 的内部成员变量常驻内存。

  

### 每次请求直接复用

  

`orchestration.py` 的 `prepare()` 方法：

  

```python

def prepare(self, question: str) -> WebsiteChatPlan:

    execution = self._query_service.run(question)   # 这条路一路下去

    # ...

```

  

调用链：`self._query_service` -> `self._pipeline` -> `self._extractor` -> `self._candidate_provider.load()`

  

### load() 的缓存逻辑

  

`candidates/cache.py`：

  

```python

class CachedCandidateProvider(CandidateProvider):

    def __init__(self, inner: CandidateProvider) -> None:

        self._inner = inner

        self._cached_inventory: CandidateInventory | None = None

  

    def load(self) -> CandidateInventory:

        if self._cached_inventory is None:       # 第一次：连 MySQL 加载

            self._cached_inventory = self._inner.load()

        return self._cached_inventory            # 之后：直接返回 Python 变量，0 次数据库调用

```

  

### 执行过程图解

  

```

第一次请求:

    load() 被调用

      -> self._cached_inventory is None?  YES

      -> self._inner.load()  --连 MySQL, 执行 6 条 SELECT DISTINCT

      -> 结果存入 self._cached_inventory

      -> 返回

  

第二次及以后的所有请求:

    load() 被调用

      -> self._cached_inventory is None?  NO

      -> 直接 return self._cached_inventory

      -> 零数据库调用，纯 Python 内存读取

```

  

### 数据流向全景

  

```

服务器启动

    │

    ├──[只执行一次] MySQLCandidateProvider

    │     执行 6 条 SELECT DISTINCT

    │     ↓

    │   CandidateInventory (Python 内存，进程存活期间常驻)

    │     │

    │   用途：给规则引擎做"名字匹配"用

    │   知道数据库里有哪些校区、学院、教师、课程名

    │

    │   ★ 这 6 条 SQL 进程生命周期内不再执行 ★

  

每次用户发消息

    │

    ├── 规则引擎拿着 Python 内存里的 CandidateInventory

    │   做匹配（模糊比对、别名映射等）

    │   0 次数据库调用

    │

    ├── MySQLCourseRepository 拿着提取出的条件

    │   执行 SELECT ... WHERE campus_name=... AND ...

    │   每次请求都会执行（查真实课程数据）

    │

    └── 拼进 LLM prompt

```

  

## 为什么这样做

  

1. **候选词表很小**：几千个值，总内存占用就几十 KB，常驻内存几乎零成本

2. **避免重复网络 IO**：每次请求省掉 6 条 `SELECT DISTINCT` 的数据库往返（~几十毫秒）

3. **模糊匹配必须全量比较**：不管数据从哪来，算法都必须遍历所有候选词才能算相似度分数

4. **数据变化极低频**：课表一个学期才更新一次，不需要实时同步，重启服务即刷新